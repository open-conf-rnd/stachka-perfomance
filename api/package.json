{
  "name": "api",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "prisma generate && tsc",
    "start": "node dist/index.js",
    "seed:bingo": "tsx src/scripts/seed-bingo-tasks.ts",
    "seed:bingo:prod": "node dist/scripts/seed-bingo-tasks.js",
    "clear-users": "tsx src/scripts/clear-users.ts",
    "test:smoke": "TG_ADMIN_USER_IDS= POSTGRES_USER=${POSTGRES_USER:-stachka} POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-stachka} POSTGRES_HOST=${POSTGRES_HOST:-localhost} POSTGRES_PORT=${POSTGRES_PORT:-5432} POSTGRES_DB=${POSTGRES_DB:-stachka} REACTION_COUNTDOWN_SECONDS=0 REACTION_LEADERBOARD_DELAY_SECONDS=0 vitest run test/smoke.test.ts",
    "test:smoke:watch": "POSTGRES_USER=${POSTGRES_USER:-stachka} POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-stachka} POSTGRES_HOST=${POSTGRES_HOST:-localhost} POSTGRES_PORT=${POSTGRES_PORT:-5432} POSTGRES_DB=${POSTGRES_DB:-stachka} REACTION_COUNTDOWN_SECONDS=0 REACTION_LEADERBOARD_DELAY_SECONDS=0 vitest test/smoke.test.ts"
  },
  "dependencies": {
    "@fastify/cors": "^11.2.0",
    "@prisma/adapter-pg": "^7.4.2",
    "@prisma/client": "^7.4.2",
    "dotenv": "^16.6.1",
    "fastify": "^5.1.0",
    "pg": "^8.20.0",
    "prisma": "^7.4.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/pg": "^8.18.0",
    "tsx": "^4.19.2",
    "typescript": "~5.6.2",
    "vitest": "^2.1.8"
  }
}
