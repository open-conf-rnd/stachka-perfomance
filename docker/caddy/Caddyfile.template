# Production: один блок — Caddy сам включит HTTPS и редирект HTTP→HTTPS (Let's Encrypt)
# DOMAIN подставляется при старте через envsubst (например event-starter.ru)
${DOMAIN}, www.${DOMAIN} {
    reverse_proxy web:80
}
